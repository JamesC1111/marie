---
import { navLinks, practiceContact } from '../lib/site';

const currentPath = Astro.url.pathname;

function isCurrent(href: string) {
  if (href === '/') {
    return currentPath === href;
  }

  return currentPath === href || currentPath.startsWith(`${href}/`);
}
---

<header class="site-header">
  <div class="container py-4">
    <div class="flex items-center justify-between gap-4">
      <a class="brand" href="/">
        <span class="brand__name">Marie Harding Counselling Service</span>
        <span class="brand__place">Kanturk, Co. Cork</span>
      </a>
      <div class="header-actions hidden md:flex">
        <a class="header-secondary" href="/contact" data-speculate="intent">
          Request appointment
        </a>
        <a class="header-call" href={`tel:+353872503743`}>
          Call {practiceContact.phoneDisplay}
        </a>
      </div>
    </div>

    <details class="mt-4 md:hidden">
      <summary class="menu-toggle">Menu</summary>
      <nav class="mobile-nav mt-3" aria-label="Primary">
        <ul class="space-y-2">
          {navLinks.map((item) => (
            <li>
              <a aria-current={isCurrent(item.href) ? 'page' : undefined} href={item.href}>
                {item.label}
              </a>
            </li>
          ))}
        </ul>
        <div class="mt-3 grid gap-2">
          <a class="mobile-nav__cta" href="/contact" data-speculate="intent">
            Request appointment
          </a>
          <a class="mobile-nav__cta mobile-nav__cta--secondary" href={`tel:+353872503743`}>
            Call {practiceContact.phoneDisplay}
          </a>
        </div>
      </nav>
    </details>

    <nav class="mt-4 hidden md:block" aria-label="Primary">
      <ul class="flex flex-wrap gap-3">
        {navLinks.map((item) => (
          <li>
            <a
              aria-current={isCurrent(item.href) ? 'page' : undefined}
              class="nav-link"
              href={item.href}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  </div>
</header>
